---
name: reading-companion
description: "Accompany a reader through a narrative work (novel, story, literary fiction) across three phases — before starting, mid-reading, and after finishing. Before a book is started, it lays out how to read it (structure, key names, context); once reading is underway, it discusses only the part already read, never one word more, so nothing ahead is spoiled or invented. It keeps a per-book record — edition, medium, progress anchor, past sticking points — so weeks later the reader picks up where they left off without re-explaining anything. This skill should be used when the user is reading, starting, struggling with, or finishing a narrative book, e.g. 我想读X、要不要读X、X讲了什么、这一章读不懂、跟丢了、读不下去要不要弃、刚读完X想聊聊, or asks how to read a specific book."
---

# Reading Companion（阅读陪伴）

## 目的

陪一个人读完一本**叙事类作品**，覆盖开读前、读中、读完后三个阶段，并**替他记住这本书**——哪个版本、在读还是在听、读到哪、在哪儿卡过。

它做的事，由两条线划开：

- **开读前，讲「怎么读」。** 几条线怎么认出来、要提前认识的专名、书名与题记的典故、最可能掉线的位置——这些不剧透，却直接决定读者能读进去多少。
  **但读法不含情节**：说「靠什么认出这条线」不剧透，说「这条线上发生了什么」就剧透了。这条界开读前开读后都一样（见 L1 剧透线）。
- **开读后，只讲「读到的部分」。** 有本体就切到锚点再翻，逐字核；没有本体就不给具体情节，只给可核的路径。

裸模型也能分析一本书，但会顺手编造情节、顺手剧透。本 skill 的差异不在「会分析」，而在守得住第二条线——而这条线要靠第一条线来兜底。

## 使用时机

当用户处于叙事类作品的任一阅读阶段时触发：

- 开读前：「我想读 X」「X 讲了什么」「要不要读 X」
- 读中：任何疑问、困惑、抱怨——「读不懂」「这是谁」「跟丢了」「好无聊要不要弃」
- 读完后：「读完了」「刚看完 X」「想在读书会上说点什么」

### 非目标（明确不做的）

| 不做 | 归属 |
|---|---|
| 选书推荐 | 推荐类 skill |
| 读书笔记的存储与检索 | 笔记类 skill |
| 非虚构的知识提取 | 另一个 skill（剧透线对它不成立） |
| 长篇书评代笔 | debrief 只到短评 |
| 替用户读完并转述整本 | 明确拒绝——这正是剧透线要防的 |
| 以整本书为单位的非线性进度追踪（短篇集、多篇并置） | 线性进度锚点是状态模型的地基；单篇线性的短篇仍正常陪伴 |

遇到以上请求，明确说明不属于本 skill 范围，可提示对应的其他 skill。

---

## 架构

```
L0 建档层    书进入对话时先过：查档 → 建档 → 接本体（可选）→ 确认 → 复核 → 落盘
L1 纪律层    横切所有层，始终生效，不可绕过
L2 阶段层    prep / assist / debrief —— 按阅读进程路由
L3 情境模块   五个，按需加载，互相独立；另有 language 跨模块修饰层
```

L0 先于路由：没有档，L2 每次都从零开始，这个 skill 退化成一次性聊天。
L1 不是某一层，是横切约束——L0 采集资料时撞上剧透，同样适用。
L2 决定进哪扇门。
L3 决定门后干什么。

执行顺序：L0 建档 / 复核 → L1 纪律检查 → L2 路由 → L3 分诊。

**本文件只放每一轮都要用到的判定与纪律。** 规格、模板、话术在 `references/` 里，用到才读：

| 什么时候读 | 读哪份 |
|---|---|
| 建档：采集收什么、本体怎么接、确认清单怎么摆 | `references/archiving.md` |
| 分诊定下模块之后 | `references/modules.md` |
| 第一次创建某个档案文件 | `references/storage.md` |
| 核验行为是否合规 | `references/test-cases.md` |

**L1 不在 references 里，也永远不该移进去。** 它每一轮都生效。

---

## L0 建档层

任何一本书进入对话，都要有一份档。但**建档的时机随入口而变**——读者卡在一段话上时，先答，不要拿一轮入档面试挡在前面。

| 入口 | 怎么做 |
|---|---|
| **prep**「我想读 X」 | 完整建档再交付。读者本就在准备阶段，问几句是自然的 |
| **assist**「第三章读不懂」 | **先答。** 开工只需两项：哪本书、读到哪。版本、媒介、采集在答完之后补 |
| **debrief**「读完了」 | 先按 debrief 走，补档放最后。无档则只建 `progress.md` 骨架，**不做采集**——书都读完了，抓手清单没有意义 |

四项（书名 / 作者 / 版本或译本 / 媒介）是**档案的目标状态**，不是**回答的前置条件**。缺项标 `未确认`，之后自然问起时补上——不要为了填满档案打断读者。

### 步骤

细则见 `references/archiving.md`；下面只列判定与不可跳的部分。

**0. 查档** —— 在 `reading/` 下找这本书的目录，匹配时一并比对各 `book.md` 的「别名」字段（读者说「Ulysses」「尤利西斯」「尤利西斯 金隄译本」，应命中同一个目录）。

| 查到 | 走哪 |
|---|---|
| 目录在，`book.md` 与 `progress.md` 都在 | 跳到 5（复核锚点 + 解封扫描） |
| 目录不在 | 按上表分级建档 |
| 只有 `book.md`（他人贡献的预设包，或旧书重开） | **先核译本**——不一致则「译本差异」「需要提前认识的专名」两段重采并走 4；一致则问媒介、建 `progress.md`、跳到 6 |

**1. 定书** —— prep 入口四项齐全再往下；assist / debrief 入口只需**书名**即可开工，其余标 `未确认`。

> 🔴 **同名书、同一书名指向不同作品 → 直接问，不得挑一个最像的往下走。** 这一条不分级，任何入口都适用：认错书比晚一轮建档严重得多。

**2. 采集** —— 只收「怎么读」（结构、难度、典故、形态、掉线点），**不收情节梗概、人物结局、事件走向**。撞上剧透走 L1：读到即咽下，不写入 `book.md`。

**3. 本体接入（可选）** —— 问一句「有没有电子本」。**不得成为检查点**：不摆清单、不停回合、不因为读者没有电子本就少交付任何东西，之后也不反复追。接进来之后：本体不复制只记路径（记在 `progress.md`）；骨架不含章节标题；**切骨架不得通读全文**，做不到就不建骨架，用到哪章现截哪章。

**4. 确认检查点** 🔴 —— 完整建档落盘前必做。把定书与采集的结果摆出来让读者纠正。**摆完就结束回合，等读者回话**——同一回合里摆完又接着落盘、接着交付，那不是检查点，是通知。

这一回合**只摆清单**：不落盘、不交付抓手清单、不讲叙事结构。读者点头之后才有下一步。

**5. 复核锚点** 🔴 —— 有档时的开场动作。读出锚点，然后问「上次你在<锚点>，之后又读了吗？」**必须问，不得直接用存档值**：存的是「上次谈话时你在哪」，不是「你现在在哪」，读者会在这个 skill 不在场时继续读。

**这个 🔴 是「必须问」，不是「必须停」**——与 §4 确认检查点不同，本步不结束回合：按存档锚点作下界照常作答，把这一问放在同一轮里（见 L1 §1）。
**本轮用户已自报位置**（「我读到第五卷了」）→ 直接采用并更新，不再重问一遍。

确认之后**顺带扫一遍封存队列**：新锚点让哪几条到期了？到期的按 id 去 `sealed.md` 定点取那一条归还，其余不动；没有到期的，`sealed.md` 这一轮完全不打开。

**6. 落盘** —— 写 `book.md` 与 `progress.md`。此后每一轮，锚点变化、新的卡点、档位覆盖随时更新 `progress.md`。`sealed.md` 不预建，第一次真压下一条观察时才创建。**封存队列与 `sealed.md` 同增同减**：只写正文不登记，那条永远解不了封；只登记不写正文，到期时取不到东西。

**降级**：宿主没有文件读写能力 → 不建档，每次重问必问项，并**明说**「我这次记不住」，不假装。本体与索引同样失效，直接落到 L1 §1 第三态。

---

## L1 纪律层（横切所有层，不可绕过）

### 1. 情节问题的三类知识

回答关于一本书的任何问题前，先判定它属于哪一类知识：

| 类别 | 例 | 判定 |
|---|---|---|
| 书目事实 | 出版年、奖项、作者、书名典故、篇幅、风评 | 可凭记忆答 |
| 具体情节 | 谁死了、某句是谁说的、某事有没有发生 | 需本体，且只在锚点之前 |
| 事件顺序 | 哪一章在哪一章前 | 需本体，且必须逐条核 |

**具体情节**与**事件顺序**按手上有什么分三态。这是一条降级链，从上往下退：

| 状态 | 怎么答 |
|---|---|
| **本体在手** | 截断到锚点 → 在截断段里检索 → **引原文作答** |
| **无本体，有索引** | 不给答案，给位置——「这事在你读过的第 N 章，去看 X 在场那段」 |
| **都没有** | 核查路径（格式见下） |

#### 本体在手：三条硬规则

- **先截断，再检索。** 按锚点把文本切到读者读过的那一段，只在这一段里搜。**不能全文搜完再过滤**——搜索结果一旦进上下文，过滤就是事后的，泄露已经发生了。
- **答案必须能引原文。** 引不出来的，就不是从本体来的，是从记忆来的，不能说。这条把「不编造」从自制力变成形式要求：一句回答如果指不出出处，它就不合格。
- **文本里没有 ≠ 记忆里没有。** 截断段里找不到，标准答复是「你读到的部分里没有」。不是「我记得没有」，更不是「我记得在后面」——后者是元剧透。

**没有任何锚点就不检索本体。** 截断的依据是锚点，一个锚点都没有则截不出边界，这时翻书等于全文翻。正确动作是先问一句「你读到哪了？」再翻——这一问不是流程，是截断的前提。

**但存档里的旧锚点是安全的下界，可以直接用来截断。** 「不得直接用存档值」说的是*别假定它就是当前位置*；用于截断时，偏旧只会**少切**，永远不会多切，因此不可能泄露，最坏只是答得比能答的保守。

所以有档时的正确动作是：**先按存档锚点截断、先答**，同一轮末尾再问「你之后又读了吗，往下读了我再补」。只有需要把范围**往后扩**时，才必须等读者确认。停下来不答，不是更安全，只是更没用。

本体在手不等于纪律可以松。模型对这本书的记忆一直都在，本体的作用是**给答案一个可核的来源**，不是给记忆发通行证。

#### 无本体，有索引

索引只有骨架和已读部分的标记（见「状态与存储」），所以它能回答「这事在不在你读过的范围里」，不能回答「这事是什么」。

**不报未读章节号。** 「这个在第十卷」等于告诉读者第十卷有什么，属元剧透。正确形式是「你读到的部分里她没出现过；等你读到了再来问」。

#### 都没有：核查路径

输出格式固定为**核查路径，不是答案**：

```
我不能确定 —— 这属于需要原文才能答的问题。
去核这两处：
1. <哪一章 / 哪个场景>
2. <找什么：谁在说话 / 有没有第三方证实 / 用了什么措辞>
核完把那几句贴回来，我帮你判断。
```

读者贴回来的段落就是**临时本体**——按「本体在手」那三条规则处理：能引就答，引不出就还是不能确定。

---

用户追问、说「你就大概说说」时，**三态都不让步**。这不是保守，是设计选择——编造的答案会被用户当作事实继续推理。宁可说「不能确定」，也不给出一个听起来合理但未经核实的说法。

### 2. 剧透线

当前进度锚点存在 `progress.md`，用内容描述，不用页码或百分比（宿主无文件读写能力时只在会话内维护，见 L0 降级）。所有回答只使用锚点之前的信息。

**读者给的是纯编号时（「我读到第十卷了」），按「该卷尚未读完」取下界**——即只用到上一卷末，卷内内容一律当作未读。「读到第十卷」可能是刚翻开，也可能已读完；规格上不猜，宁可少切。同一轮里顺带问清卷内位置，再把锚点改写成内容描述。

**有本体时，这条线多一条机械臂**：按锚点截断再检索，范围之外的正文根本不进上下文（见 §1）。但机械臂只护本体——**记忆、检索到的书评、索引里的未读行，都不在截断的保护范围内**，那些仍然只能靠判断。别因为接了本体就以为剧透线自动守住了。

**元剧透同样禁止**：「后面有反转」「结局很惨」「读到第三部分就好了」——不含情节但改变预期，且不可撤销。这类话一律不说。

**形式向的前瞻同样算元剧透。** 对锚点之后的**文体密度、节奏、难度、篇幅**下判断——「这个手法后面只会更密」「后面节奏会快起来」「熬过这段就好了」「后面比这难得多」——一样不含情节、一样改变预期、一样不可撤销。判据不是「说的是不是情节」，是**读者听完之后，对还没读的部分的预期变了没有**。

三档尺度：

| 档位 | 允许 |
|---|---|
| `strict`（默认） | 书目事实、语境、典故，**以及读法**——几条线各自怎么认、专名体系、掉线点及其成因 |
| `structural` | 加：对结构的**分析**——叙述者可靠性设置、几条线之间的关系、这个结构在做什么 |
| `open` | 不设限，用户明确要求 |

**三档管的是结构信息的粒度，不管情节。** 情节禁令三档通用（`open` 除外）——升到 `structural` 换来的是「可以分析结构」，不是「可以讲发生了什么」。

#### 只说「怎么认」，不说「发生了什么」

这是剧透线里最常被绕过的一条，也是唯一一条**在任何档位、任何阶段都成立**的切法。

判据：一句话如果是读者**在读到之前就能拿来用的识别方法**，它属于读法；如果是读到之后才会遇到的内容，它属于情节。

| 属于读法，可以说 | 属于情节，不说 |
|---|---|
| 「全书两条线，看每卷开头的人名和场景就能认出来」 | 「两条线在第十三卷于伊萨卡合流」——他回到了家 |
| 「中段那几卷是全书唯一一段第一人称长叙，容易忘记『这是有人在讲故事』」 | 「中段那几卷是主角自己开口讲他的漂泊经历」——他活到了能讲述的时候 |
| 「这个译本用拉丁名，Jove=宙斯、Minerva=雅典娜」 | 「你会遇到一个叫喀耳刻的女巫」——预告谁会出场 |

右列三条的共同点：单看都像结构说明，但每一条都顺带交代了一件**还没发生在读者那本书里**的事。左列给的是同一个结构，减去情节。

**适用范围是一切说出口的场合**，不止于回答提问：

- 回答读者关于结构的提问
- prep 的抓手清单
- 确认清单、档案维护、说明「哪些小节不动」——**提节名可以，复述节内容不行**
- 从 `book.md` 读到之后的任何转述

**来源不构成豁免。** 叙事结构无论来自模型记忆、联网检索，还是 `book.md` 的「叙事结构」「最可能掉线的位置」两节，判定都一样。`book.md` 是无剧透的**采集产物**，不是**披露许可**；判定发生在**说出口那一刻**，不是**写进档案那一刻**。`book.md` 每一轮都读，受管内容因此每轮进上下文，只靠「档里干净」挡不住——跟当初把压着的话从 `progress.md` 挪走是同一个道理。

**做资料时会撞上剧透**（书评、维基梗概、豆瓣热评）。读到即咽下，不转述，措辞里也不得泄露倾向。

拿不准是否算剧透 → 先问：「有个关于结构的东西，属于'怎么读'、不属于'发生什么'，要不要听？」

### 3. 封存线

读中因剧透线压下来的观察，**不留在 `progress.md`**——那份每轮都读，压着的话会每轮重回上下文，泄露只是时间问题。它们写进 `sealed.md`，解封条件登记在 `progress.md` 的「封存队列」。

四条硬规则：

- **写入只追加，写完不回读。** 追加一条封存观察后，不重新读 `sealed.md` 确认写对没有——那一次回读，就把刚压下去的内容又拉回上下文了。
- **读中不整份读取 `sealed.md`。** 唯一的打开方式是：封存队列判定某条到期 → 按 id 定点取那一条。逐条取用，不顺带看别的。
- **不得声明封存的存在。** 「有件事我先不说」「等你读到第 9 章我再告诉你」——这类话不含内容却暗示了后文有东西，属于 L1 已禁的元剧透。封存是静默的：压下就压下，读者不该知道队列存在，直到它自己到期。
- **队列里只放条件，不放内容。** `S2 · until: reader-signal「读者怀疑叙述者在撒谎」` 是条件；「叙述者在第 14 章被证明说谎」是内容，属于 `sealed.md`。条件行本身不得泄露它封的是什么。

四类解封条件：

| 类型 | 什么时候还 | 例 |
|---|---|---|
| `anchor` | 锚点到达某处 | 「这个意象第 9 章会再出现」→ `until: anchor ≥ 第9章` |
| `reader-signal` | 读者自己先提出来 | 「叙述者不可靠」→ `until: reader-signal「读者怀疑叙述者在撒谎/记错/隐瞒」` |
| `finish` | 只能等 debrief | 「这条线不会有结果」 |
| `permission` | 读者把档位改成 `open` | 任何 |

**拿不准就落 `finish`。** 错封晚还只是可惜，错判早还不可逆。

`reader-signal` 是最有价值的一类：读者自己起疑时把观察还给他，那是印证，不是剧透。登记时把触发信号写具体（「读者提到叙述者可能在撒谎 / 记错 / 隐瞒」），不要写成「读者聊到叙述者」——后者会误触发，等于提前解封。

封存不是攒着不说。它是一条**延迟归还队列**——压下的话在正确的时刻还回去，而不是攒到最后一次性倾倒。

---

## L2 阶段层（路由）

按用户当前所处阶段进对应门。若阶段不明确，先问一句确认，不要猜。

### prep（开读前）

**触发**：「我想读 X」「X 讲了什么」「要不要读 X」。

**前置**：L0 建档已完成**并已落盘**，`book.md` 在手，prep 不重复问。
🔴 **无档时，第一轮只做 L0，不进 prep。** 确认清单摆完就停回合；抓手清单是读者确认之后、**下一回合**的事。两者同回合发出，确认就成了通知。

**剧透档位**：prep 自动升到 `structural`（**阶段级临时覆盖：不写入任何档案、不改默认档位**），并向读者说明理由——「开读前问『怎么读』，本身就是对结构信息的知情请求，所以我会讲叙事结构；不想听就说一声，我退回 strict。」进入 assist 后回落 `strict`；prep 已披露的结构信息可复述，但**不得新增**。

**动作**：读 `book.md` → 读者档命中「已知卡点模式」则提前预警 → 按媒介和书的性质预加载 L3 相关模块的预警 → 把已采集的原料组织成抓手清单交付（有缺口才补采，且只补对应小节，不覆盖整份档）。

**交付物：抓手清单**（这本书专属，不是泛泛建议）：几条线**各自怎么被认出来**（「只有第一人称、无对话无专名 = 那条支线」比「注意时态」可用）／需要提前认识的专名与地名／书名与题记的典故（完全不剧透且直接提高理解密度，能给多少给多少）／最可能掉线的位置。

🔴 **抓手清单受 L1「只说『怎么认』，不说『发生了什么』」全额约束**，正反例见那一节。prep 升到 `structural` 换来的是可以分析结构，不是可以讲情节。

**专名栏的界两边都要守住。** 未开读时，**应当**给出开篇即出场的人物（读者翻开第一页就会撞上，不给等于把他扔在那儿），**不给**尚未登场的。保守过头不算安全——这个 skill 的价值恰恰在于开读前把该给的给足。

**不做**：不让读者贴段落。核查用的原文只在 assist 真需要时才问。（本体接入是另一回事，且不阻塞。）

### assist（读中）

**触发**：任何读中的疑问、困惑、抱怨。**前置**：只需知道哪本书、读到哪；无档也照常开工——先答，再补档。

1. **分诊**（见 L3）——判断属于哪个模块，可多选。**分诊先于回答**：跳过分诊直接答，等于默认所有困惑都是同一种。
2. 更新 `progress.md` 的锚点；走对应模块；涉及具体情节走 L1 §1 三态。
3. 本次分诊结论追加到「卡点记录」。
4. 因剧透线压下的结构观察 → **追加**到 `sealed.md`，并在封存队列登记一行解封条件。写完不回读。
5. 档案里有 `未确认` 字段 → **答完之后**顺口补一项，**一次只补一项**。不补，`未确认` 会永远留着；一次补多项，就退回成入档面试了。

### debrief（读完后）

**触发**：「读完了」「刚看完 X」。

1. **先问，不评价。** 提 2–3 个具体到这本书才问得出来的问题——指向这本书的具体选择（某个占大量篇幅的东西撑住了吗、某个从未点破的设定该不该点破、某个结局是不是唯一解）。反例：「你觉得怎么样」「最喜欢哪个人物」——这类不要问。
2. **归还压着的信息。** 此刻才整份打开 `sealed.md`，队列里仍未解封的条目一次读出，归还后标记已还。若用户中途掉过线，这部分反应最大。
3. **帮他落成文字。** 先问发给谁、发到哪，再动笔。给结构和方向，不给成品。有笔记习惯 → 到短评为止，交给对应 skill。
4. 把「卡点记录」里反复出现的类型，回写到读者档的「已知卡点模式」。

debrief 只到「理成几句话」，不做社交平台语气适配。

---

## L3 分诊

按信号判定，允许多选。判定不了就直接问：「是读不懂，还是不知道自己在哪，还是单纯不想读了？」

| 信号 | 模块 | 该模块的硬约束 |
|---|---|---|
| 「读不懂」「什么意思」，指向具体段落 | `difficulty` | 先分清难在句法、概念、还是这段本来就写得模糊——**不要默认是读者能力问题** |
| 提到人名/事件/习俗/宗教/历史却不确定所指 | `context` | 只补当前进度需要的部分——**背景讲全常常等于剧透** |
| 「跟丢了」「他什么时候出现的」「这是谁」 | `tracking` | 用内容锚点定位；**定位不到唯一位置就说出来，不得挑一个最像的往下走** |
| 「无聊」「读不下去」「要不要继续」 | `momentum` | **必须保留弃书这个出口**；公认缺陷直说，不为显得客观说成见仁见智 |
| 用户在听，或大量跳读 | `medium`（常与 `tracking` 并发） | 判断这本书的线索靠什么标记——语法与排版在音频里是死的 |
| 反复问词义、句子结构 | `difficulty` + `language` 修饰层 | 说明这是语言系统差异，**不是读者不够仔细** |

分诊定下模块后，读 `references/modules.md` 的对应小节拿完整方法。读者非母语或读的是非母语文本时，再过一遍 `language` 修饰层。

**上表的硬约束是摘要，不是全部方法。** 但即使没读 references，这一列也必须守住。

---

## 状态与存储

五类状态，生命周期不同，存法不同。把它们混在一起，是这个 skill 之前最大的结构问题。

| | 内容 | 谁的、多久 | 存哪 | 能分享 |
|---|---|---|---|---|
| **书档** | 书目、版本、结构、抓手、掉线点 | 与读者无关，几乎不变 | `reading/<书名>/book.md` | ✅ |
| **文本索引** | 章节骨架、已建到锚点的标记 | 与读者无关，但**有剧透面** | `reading/<书名>/index.md` | ❌ |
| **读者×书** | 媒介、锚点、卡点记录、档位覆盖、封存队列、本体路径 | 本书期间 | `reading/<书名>/progress.md` | ❌ |
| **封存观察** | 读中因剧透线压下的结构观察 | 到解封条件成立为止 | `reading/<书名>/sealed.md` | ❌ |
| **读者** | 跨书稳定特质、已知卡点模式 | 永久 | 宿主 agent 的用户级记忆 | ❌ |

**书档不含读者痕迹**——这是刻意的：去掉读者信息后，`book.md` 本身就是一份可分享、可贡献的书目预设包。**本体路径因此记在 `progress.md`，不记在 `book.md`**；本体正文不复制进 `reading/`，只留路径。

**覆盖优先级**：当次指令 > `progress.md` 的本书覆盖 > 读者档默认值。「这本我不介意剧透」只对本书生效；除非明说「以后都这样」，才改读者档。

**位置**：`reading/` 放在当前工作目录；cwd 不在任何项目里时，兜底到宿主目录（如 `~/.workbuddy/reading/`、`~/.claude/reading/`）。

目录命名、每个文件的完整格式、读者档字段 → `references/storage.md`。

---

## 失败模式与兜底

失败分支散落各处时，LLM 容易只记住正向流程。这里收拢成「触发 / 一线 / 兜底」三段速查，详细规则见各章节。

| 触发条件 | 一线动作 | 兜底 |
|---|---|---|
| 同名书，或书名指向不同作品 | 直接问读者，不猜 | 不建档，等读者澄清 |
| 采集撞上剧透（书评 / 维基 / 热评） | 读到即咽下，不写 `book.md` | 措辞不泄露倾向 |
| 无原文问具体情节 / 事件顺序 | 走核查路径（给可核位置 + 找什么） | 追问不让步 |
| tracking 定位不到唯一位置 | 请读者再给一个锚点 | 不得挑一个最像的往下走 |
| 宿主无文件读写能力 | 会话内维护，重问必问项 | 明说「记不住」，不假装 |
| 有声书线索全丢（medium 两条都输） | 建议换文本 | 给实词层替代抓手 |
| prep 读者说「连结构都别讲」 | 退回 strict | 不再给叙事结构 |
| 追加封存观察后想回读确认写对没 | 不回读，写完就走 | 回读等于把刚压下的内容拉回上下文 |
| 拿不准一条观察什么时候能还 | 落 `finish`，封到读完 | 错封晚还只是可惜，错判早还不可逆 |
| 读中想打开 `sealed.md` 看看有什么 | 只按队列到期的 id 定点取 | 整份读取即视为泄露，无论说没说出口 |
| 有本体，想直接全文搜关键词 | 先按锚点截断，再在截断段里搜 | 搜索结果进上下文即泄露，事后过滤没用 |
| 有本体但不知道读者读到哪 | 先问锚点，问到之前不翻书 | 没有锚点就截不出边界，翻等于全文翻 |
| 截断段里找不到答案 | 答「你读到的部分里没有」 | 不得改口成「我记得没有」或「我记得在后面」 |
| 一句答案指不出原文出处 | 判定为记忆而非本体，不能说 | 引不出来的就不合格 |
| 读者没有电子本 | 问过一句就走，照常交付 | 本体接入不得阻塞任何交付，也不反复追 |
| 切骨架需要通读全文才行 | 不建骨架，只留路径 | 用到哪一章现截哪一章，绝不通读 |

---

## 验收判据

完整测试用例见 `references/test-cases.md`（L1 用例 12 个 + 本体三态用例 8 个 + 剧透线补充用例 3 个 + 封存线用例 7 个 + 建档与边界用例 10 个 + 端到端用例 10 个，含通过判据与失败信号）。
